
void _Precache()
{
	NPCData data;
	strcopy(data.Name, sizeof(data.Name), "");
	strcopy(data.Plugin, sizeof(data.Plugin), "npc_");
	strcopy(data.Icon, sizeof(data.Icon), "sea_");
	data.IconCustom = true;
	data.Flags = MVM_CLASS_FLAG_NORMAL|MVM_CLASS_FLAG_MINIBOSS;
	data.Category = Type_Seaborn;
	data.Func = ClotSummon;
	NPC_Add(data);
}

static any ClotSummon(int client, float vecPos[3], float vecAng[3], int ally)
{
	return (client, vecPos, vecAng, ally);
}

		func_NPCDeath[npc.index] = _NPCDeath;
		func_NPCOnTakeDamage[npc.index] = _OnTakeDamage;
		func_NPCThink[npc.index] = _ClotThink;


Generic_OnTakeDamage